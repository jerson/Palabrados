{
  "name": "Palabrados",
  "version": "0.1.0",
  "private": true,
  "main": "main.js",
  "devDependencies": {
    "electron": "^1.4.12",
    "electron-packager": "^8.4.0",
    "react-scripts": "0.8.4"
  },
  "dependencies": {
    "react": "^15.4.1",
    "react-dom": "^15.4.1",
    "soundmanager2": "^2.97.20150601-a"
  },
  "scripts": {
    "electron": "electron .",
    "build:app:mac": "npm run build && electron-packager ./ Palabrados --out=releases --platform=darwin",
    "build:app:windows": "npm run build && electron-packager ./ Palabrados --out=releases --platform=win32",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
